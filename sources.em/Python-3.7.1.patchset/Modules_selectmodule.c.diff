--- Python-3.7.1/Modules/selectmodule.c	2018-10-20 08:04:19.000000000 +0200
+++ Python-3.7.1.em/Modules/selectmodule.c	2018-11-17 07:18:26.274870728 +0100
@@ -266,7 +266,11 @@
     do {
         Py_BEGIN_ALLOW_THREADS
         errno = 0;
+#if defined(__EMSCRIPTEN__)
+        n = select(max, &ifdset, &ofdset, NULL, tvp); // &efdset exceptfds not supported (assert)
+#else
         n = select(max, &ifdset, &ofdset, &efdset, tvp);
+#endif
         Py_END_ALLOW_THREADS
 
         if (errno != EINTR)
