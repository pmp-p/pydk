--- Python-3.7.1/Lib/subprocess.py	2018-10-20 08:04:19.000000000 +0200
+++ Python-3.7.1.em/Lib/subprocess.py	2018-11-17 07:18:26.262871122 +0100
@@ -40,10 +40,9 @@
 getstatusoutput(...): Runs a command in the shell, waits for it to complete,
     then returns a (exitcode, output) tuple
 """
-
 import sys
 _mswindows = (sys.platform == "win32")
-
+_emscripten = (sys.platform == "emscripten")
 import io
 import os
 import time
@@ -149,7 +148,8 @@
                                lpAttributeList=attr_list)
 
 else:
-    import _posixsubprocess
+    if not _emscripten:
+        import _posixsubprocess
     import select
     import selectors
     import threading
