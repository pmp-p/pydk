--- cpython-3.7.1rc1/Lib/subprocess.py	2018-09-26 08:16:09.000000000 +0200
+++ cpython-3.7.1rc1.em/Lib/subprocess.py	2018-10-09 03:10:41.515197398 +0200
@@ -40,10 +40,9 @@
 getstatusoutput(...): Runs a command in the shell, waits for it to complete,
     then returns a (exitcode, output) tuple
 """
-
 import sys
 _mswindows = (sys.platform == "win32")
-
+_emscripten = (sys.platform == "emscripten")
 import io
 import os
 import time
@@ -149,7 +148,8 @@
                                lpAttributeList=attr_list)
 
 else:
-    import _posixsubprocess
+    if not _emscripten:
+        import _posixsubprocess
     import select
     import selectors
     import threading
