--- cpython-bpo-30386/Modules/selectmodule.c	2017-12-10 15:07:21.000000000 +0100
+++ cpython-bpo-30386.em/Modules/selectmodule.c	2018-04-13 07:09:38.031076331 +0200
@@ -266,7 +266,11 @@
     do {
         Py_BEGIN_ALLOW_THREADS
         errno = 0;
+#if defined(__EMSCRIPTEN__)
+        n = select(max, &ifdset, &ofdset, NULL, tvp); // &efdset exceptfds not supported (assert)
+#else
         n = select(max, &ifdset, &ofdset, &efdset, tvp);
+#endif
         Py_END_ALLOW_THREADS
 
         if (errno != EINTR)
