--- panda3d-webgl-port/panda/src/express/virtualFileSystem.cxx	2017-09-06 11:21:01.000000000 +0200
+++ panda3d-webgl-port.py37/panda/src/express/virtualFileSystem.cxx	2018-04-22 19:02:37.342073819 +0200
@@ -1149,6 +1149,24 @@
   // Also transparently look for a regular file suffixed .pz.
   Filename strpath_pz = strpath + ".pz";
 
+//PMPP
+#ifdef EMSCRIPTEN
+    #ifdef CPPPARSER
+        #warning vfs hack at virtualFileSystem.cxx:1155
+    #else
+        if ( strpath_pz.exists() ){
+            // compressed file already here
+        } else {
+            if ( strpath.exists() ){
+                // raw file is already here
+            } else {
+                int rx = EM_ASM_INT( {  return Module.callfs( Pointer_stringify($0) ); }, strpath.c_str() );
+            }
+        }
+    #endif
+#endif
+//PMPP
+
   // Now scan all the mount points, from the back (since later mounts override
   // more recent ones), until a match is found.
   PT(VirtualFile) found_file = NULL;
