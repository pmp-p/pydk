--- Python-3.8.0b3/Modules/selectmodule.c	2019-07-29 15:26:01.000000000 +0200
+++ Python-3.8.0b3.em/Modules/selectmodule.c	2019-08-29 10:16:32.886094938 +0200
@@ -322,7 +322,11 @@
     do {
         Py_BEGIN_ALLOW_THREADS
         errno = 0;
+#if defined(__EMSCRIPTEN__)
+        n = select(max, &ifdset, &ofdset, NULL, tvp); // &efdset exceptfds not supported (assert)
+#else
         n = select(max, &ifdset, &ofdset, &efdset, tvp);
+#endif
         Py_END_ALLOW_THREADS
 
         if (errno != EINTR)
